// appweb4/front/src/modules/order/order.tsx (O la ruta y nombre real que encuentres)

import React from 'react';

const Order = () => { // Ajusta este nombre (Order) si tu componente se llama diferente (ej: Orders)
  return (
    <div>
      <h1>Esta es la página de Órdenes</h1>
      {/* Puedes agregar más texto o elementos aquí si lo deseas, por ejemplo: */}
      {/* <p>Gestión de pedidos y órdenes.</p> */}
    </div>
  );
};

export default Order; // Ajusta este nombre (Order) si tu componente se llama diferente