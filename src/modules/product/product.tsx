// appweb4/front/src/modules/product/product.tsx (O la ruta y nombre real que encuentres)

import React from 'react';

const Product = () => { // Ajusta este nombre (Product) si tu componente se llama diferente (ej: Products)
  return (
    <div>
      <h1>Esta es la página de Productos</h1>
      {/* Puedes agregar más texto o elementos aquí si lo deseas, por ejemplo: */}
      {/* <p>Visualización de todos nuestros productos.</p> */}
    </div>
  );
};

export default Product; // Ajusta este nombre (Product) si tu componente se llama diferente